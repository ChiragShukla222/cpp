#include<iostream>
#include<cstring>
using namespace std ;
int main(){
    char c [7]="hellow";
//    strrev(c[]);
    cout<<strrev(c);
    //int arr[5];
   /* int a=0;
    cout<<"enter the number ";
    int len  = sizeof(arr)/sizeof(arr[0]);
    for(int i = 0 ; i < len ; i++){
        cin>>arr[i];
    }
    for(int i = 0 ; i < len ; i++){
        if(arr[i]>=a){
            a = arr[i];

        }
    }
    cout<<"largest number is :"<<a;
    int a = arr[0];
    cout<<"enter the numbers ";
    int len = sizeof(arr)/sizeof(arr[0]);
    for(int i = 0 ; i < len ;i++){
        cin>>arr[i];
    }
    for(int i = 0 ; i <  len ; i++){
        if(arr[i]<=a){
            a = arr[i];
        }
    }
    cout<<a;
    int arr[8];
    cout<<"enter the eight elements :";
    int len = sizeof(arr)/sizeof(arr[0]);
    for(int i = 0 ; i <  len  ; i++){
        cin>>arr[i];
    }
    for(int i = 0 ; i < len ; i++){
        if(arr[i]%2!=0){
            arr[i]= arr[i]*arr[i]*arr[i];
            cout<<arr[i]<<"\t";
        }
    }
    char c [7]= "chirag";
    for(int i = 0 ; c[i] != NULL ; i++){
        if(c[i]>=97&&c[i]<=120){
            c[i]= c[i]-32;
            cout<<c[i];
        }
        else if(c[i]>=65&&c[i]<=90){
            c[i]=c[i]+32;
            cout<<c[i];
        }
    }*/
}
