#include<iostream>
using namespace std ;
int main(){
    int crr[0] ;
    int  m ;
    int len = sizeof(crr)/sizeof(crr[0]);
    for(int i = 0 ; i < len ; i++){
        cin>>crr[i];
    }
    for(int i = 0 ; i <len ; i++){
        if()
    }




}
